<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wagchoir.crm.workbench.dao.ActivityRemarkDao">
    <select id="selectByIds" resultType="int">
        select count(*) from tbl_activity_remark
        where activityId in
        <foreach collection="array" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>

    <delete id="deleteByIds">
        delete from tbl_activity_remark
        where activityId in
        <foreach collection="array" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <select id="getRemarkList" resultType="ActivityRemark">
        select id,noteContent,createTime,createBy,editTime,editBy,editFlag
        from tbl_activity_remark where activityId=#{id} order by createTime desc
    </select>

    <delete id="deleteRemarkById">
        delete from tbl_activity_remark where id=#{id}
    </delete>

    <insert id="saveNewRemark">
        insert into
        tbl_activity_remark(id,noteContent,createTime,createBy,editFlag,activityId)
        values(#{id},#{noteContent},#{createTime},#{createBy},#{editFlag},#{activityId})
    </insert>

    <update id="updateRemark">
        update tbl_activity_remark
        set noteContent=#{noteContent},
        editFlag=#{editFlag},editTime=#{editTime},editBy=#{editBy}
        where id=#{id}
    </update>
</mapper>